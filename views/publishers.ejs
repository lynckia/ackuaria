


<div id="pubsContainer" class="container">
	<div id="roomInfo">
		<div id="roomName">Room <%= roomName %></div><div class="back"><span class="light blue">BACK TO </span><span id="back" class="bold dark">ROOMS</span></div>
		<div id="roomId"><%= roomID %></div>
		<div id="pubsCount"> <span id="numberStreams">0</span> STREAMS <i class="fa fa-users"></i></div>
	    <div class="btn-group btn-toggle" id="switch">
	        <button class="btn btn-sm btn-default" id="fails"> <span class="glyphicon glyphicon-warning-sign"></span> </button>
	        <button class="btn btn-sm btn-primary active" id="grid"><span class="glyphicon glyphicon-th-large"></span></button>
	        <button class="btn btn-sm btn-default" id="list"><span class="glyphicon glyphicon-menu-hamburger"></span> </button>
	    </div>
		<div class="row" id="search">
	       <div id="custom-search-input">
	            <div class="input-group col-md-12">
	                <input id="searchBar" type="text" class="  search-query form-control" placeholder="Search" />
	                <span class="input-group-btn">
	                    <button class="btn btn-danger" type="button">
	                        <span class=" glyphicon glyphicon-search"></span>
	                    </button>
	                </span>
	            </div>
	        </div>
		</div>

	</div>

	<div id="publishers" class="row row-centered">
	
	</div>

</div>


<script>

  var room = <%- JSON.stringify(room) %>;
  var roomID = <%- JSON.stringify(roomID) %>;
  var roomName = <%- JSON.stringify(roomName) %>;
  var streams = <%- JSON.stringify(streams) %>;
  var states = <%- JSON.stringify(states) %>;
  paintPublishersGrid();
  



</script>
