
<div id="pubsContainer" class="container">
	<div id="agentnfo">
		<div class="back"><span class="light blue">BACK TO </span><span id="back" class="bold dark">AGENTS</span></div>
		<div id="agentId"><%= agentID %></div>
		<div id="pubsCount"> 
			<span id="numberStreams">0</span> STREAMS <i class="fa fa-users"></i>
		</div>

	    <div class="btn-group btn-toggle" id="switch">

	        <button class="btn btn-sm btn-primary active" id="grid" data-toggle="tooltip" data-placement="top" title="Show streams in grid"><span class="glyphicon glyphicon-th-large"></span></button>
	        <button class="btn btn-sm btn-default" id="list" data-toggle="tooltip" data-placement="top" title="Show streams in list"><span class="glyphicon glyphicon-menu-hamburger"></span> </button>
	    </div>
		<div class="row" id="search">
	       <div id="custom-search-input">
	            <div class="input-group col-md-12">
	                <input id="searchBar" type="text" class="  search-query form-control" placeholder="Search" />
	                <span class="input-group-btn">
	                    <button class="btn btn-danger" type="button">
	                        <span class=" glyphicon glyphicon-search"></span>
	                    </button>
	                </span>
	            </div>
	        </div>
		</div>

	</div>

	<div id="publishers" class="row row-centered">
	
	</div>

</div>

<div class="modal fade" id="publisherModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title" id="exampleModalLabel"><span id="pubState" class="fa fa-circle red"></span><span class="bold dark" id="username"></span> - <span class="bold dark" id="userid"></span></h4>
      </div>
      <div class="modal-body">
		 <div id="pubModal" class="row">

	        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12" id="dataModal">
				
	        </div>
      </div>
    </div>
  </div>
</div>
</div>
<script>

  var agent = <%- JSON.stringify(agent) %>;
  var agentID = <%- JSON.stringify(agentID) %>;
  var agentMetadata = <%- JSON.stringify(agentMetadata) %>;
  var agentStats = <%- JSON.stringify(agentStats) %>;
  var streams = <%- JSON.stringify(streams) %>;
  paintPublishersGrid();
  



</script>
